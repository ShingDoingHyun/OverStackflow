<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bit.op.osf.tagBoard.mapper.QuestionBoardMapper">
	<select id="boardList" parameterType="com.bit.op.osf.tagBoard.model.Board">
        insert into questionboard(memId, content, title) value ('test', #{content}, #{title}); 
    </select>
</mapper>